var fs = require("fs");
var text = fs.readFileSync("./proteinGroups.txt").toString().split("\n");
// General Code
var protein = read("proteinGroups.txt").split("\t");
for (var i = 0; i < protein.length; ++i) {
          protein[i] = protein[i].trim();
       }
for (var i = 0; i < names.length; ++i) {
var name = protein[i].split("\t");
}}
